pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                echo 'Running build automation'
                node ./
                echo 'Application is running'
            }
        }
    }
}
